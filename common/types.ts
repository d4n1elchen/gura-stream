export type ChatMessage = {
  userId: string
  message: string
}

export type PlaybackState = {
  videoId: string
  time: number
}

export type UserInfo = {
  id: string
  username: string
  avatar: string
}
